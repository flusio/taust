<!DOCTYPE html>
<html lang="en-GB">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="initial-scale=1.0" />

        <link rel="stylesheet" href="<?= url_static('stylesheets/application.css') ?>">

        <title><?= $title ?> · taust</title>
    </head>

    <body>
        <div class="layout">
            <header class="layout__header header">
                <nav class="header__container">
                    <a class="header__link header__link--home" href="<?= url('home') ?>">
                        taust
                    </a>

                    <div class="header__separator"></div>

                    <?php if ($current_user): ?>
                        <?= $this->protect($current_user->username) ?>

                        <form method="post" action="<?= url('logout') ?>">
                            <input type="hidden" name="csrf" value="<?= csrf_token() ?>" />
                            <button type="submit" class="button--ghost">
                                <?= _('Logout') ?>
                            </button>
                        </form>
                    <?php endif; ?>
                </nav>
            </header>

            <main class="layout__content">
                <?= $this->safe('content') ?>
            </main>
        </div>
    </body>
</html>
