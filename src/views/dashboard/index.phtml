<?php $this->layout('base.phtml', ['title' => _('Dashboard')]); ?>

<a href="<?= url('new domain') ?>">
    <?= _('Add a new domain') ?>
</a>

<ul>
    <?php foreach ($domains as $domain): ?>
    <li>
        <a href="<?= url('show domain', ['id' => $domain->id]) ?>">
            <?= $this->protect($domain->id) ?>
        </a>
    </li>
    <?php endforeach; ?>
</ul>
